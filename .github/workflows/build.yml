name: Buffalo WXR-2533DHP2 – Stable ImmortalWrt (NSS Final)

on:
  workflow_dispatch:

env:
  TZ: Asia/Ho_Chi_Minh
  FORCE_UNSAFE_CONFIGURE: 1

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 480

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup Build Environment
      run: |
        sudo apt update
        sudo apt install -y build-essential clang flex bison g++ gawk \
          gcc-multilib g++-multilib gettext git libncurses5-dev \
          libssl-dev python3-distutils rsync unzip zlib1g-dev \
          file wget time

    - name: Clone ImmortalWrt
      run: |
        git clone -b master https://github.com/immortalwrt/immortalwrt openwrt

    - name: Update & Install Feeds
      run: |
        cd openwrt
        ./scripts/feeds update -a
        ./scripts/feeds install -a

    # ============================================================
    # CONFIG – FINAL STABLE (Tối ưu cho Buffalo WXR-2533DHP2)
    # ============================================================
    - name: Configure Firmware
      run: |
        cd openwrt

        cat << 'EOF' > .config
        # ===== Target =====
        CONFIG_TARGET_ipq806x=y
        CONFIG_TARGET_ipq806x_generic=y
        CONFIG_TARGET_ipq806x_generic_DEVICE_buffalo_wxr-2533dhp2=y

        # ===== Build both initramfs + sysupgrade =====
        CONFIG_TARGET_ROOTFS_INITRAMFS=y

        # ===== Base system =====
        CONFIG_PACKAGE_dropbear=y
        CONFIG_PACKAGE_ip-full=y
        CONFIG_PACKAGE_curl=y
        CONFIG_PACKAGE_htop=y

        # ===== Ethernet (required on IPQ806x) =====
        CONFIG_PACKAGE_kmod-qca-nss-dp=y

        # ===== NSS Hardware Acceleration (FINAL) =====
        CONFIG_PACKAGE_kmod-qca-nss-drv=y
        CONFIG_PACKAGE_kmod-qca-nss-drv-netlink=y
        CONFIG_PACKAGE_kmod-qca-nss-drv-pppoe=y
        CONFIG_PACKAGE_kmod-qca-nss-drv-bridge=y
        CONFIG_PACKAGE_kmod-qca-nss-ecm=y
        CONFIG_PACKAGE_kmod-qca-nss-ecm-standard=y
        CONFIG_PACKAGE_nss-firmware-ipq806x=y
        CONFIG_NSS_FIRMWARE_VERSION_11_4=y

        # ===== Wi-Fi (STOCK – ổn định cho Buffalo) =====
        CONFIG_PACKAGE_kmod-ath10k=y
        CONFIG_PACKAGE_ath10k-firmware-qca9984=y
        CONFIG_PACKAGE_ath10k-board-qca9984=y

        # ===== LuCI =====
        CONFIG_PACKAGE_luci=y
        CONFIG_PACKAGE_luci-ssl=y
        CONFIG_PACKAGE_luci-i18n-base-vi=y
        CONFIG_PACKAGE_luci-app-opkg=y
        CONFIG_PACKAGE_luci-app-sqm=y
        CONFIG_PACKAGE_luci-app-ttyd=y
        CONFIG_PACKAGE_luci-app-cpufreq=y

        # ===== USB & Storage (Fixed) =====
        CONFIG_PACKAGE_kmod-usb3=y
        CONFIG_PACKAGE_kmod-usb-dwc3-qcom=y
        CONFIG_PACKAGE_kmod-usb-storage=y
        CONFIG_PACKAGE_kmod-fs-ntfs3=y

        # ===== Optimization & Conflict Fix =====
        # CONFIG_PACKAGE_kmod-nft-offload is not set
        CONFIG_PACKAGE_kmod-tcp-bbr=y
        CONFIG_PACKAGE_irqbalance=y
        EOF

        make defconfig

    - name: Download sources
      run: |
        cd openwrt
        make download -j$(nproc)

    - name: Build firmware
      run: |
        cd openwrt
        make -j$(nproc) || make -j1 V=s

    - name: Upload firmware
      uses: actions/upload-artifact@v4
      with:
        name: Buffalo-WXR2533DHP2-Final
        path: |
          openwrt/bin/targets/ipq806x/generic/*initramfs-uImage.itb
          openwrt/bin/targets/ipq806x/generic/*sysupgrade.bin
